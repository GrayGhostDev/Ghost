apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Production overlay — placeholder for future GKE deployment.
# Secrets will use External Secrets Operator with GCP Secret Manager
# and Workload Identity for authentication (no key files).

resources:
  - ../../base

namespace: ghost-backend

# Production images are pushed to Artifact Registry
images:
  - name: ghost-backend
    newName: us-east1-docker.pkg.dev/sylvan-flight-476922-m7/ggdc-images/ghost-backend
    newTag: latest
